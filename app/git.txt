Conseguir ssh para GitLab
ssh-keygen -t rsa -b 4096

--Clave ssh
cat ~/.ssh/id_rsa.pub


MODELO VISTA CONTROLADOR

M = MODELO (BASE DE DATOS)
V = VISTA (PANTALLAS)
C = CONTROLADOR (FUNCIONES)

M = MODELOS (BASE DE DATOS)
V = VISTA (FUNCIONES
T = TEMPLATES (PANTALLAS)
